version: '3.9'

# MemPalace standalone
# Premier lancement : bash setup.sh
# Commandes : make start / make stop / make logs / make reset

services:

  # ── Qdrant ──────────────────────────────────────────────────
  qdrant:
    image: qdrant/qdrant:v1.18.0
    ports:
      - "6334:6333"
    volumes:
      - qdrant_data:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:6333/ >/dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10

  # ── MinIO (S3-compatible object storage) ─────────────────────
  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    command: server /data --console-address ":9103"
    ports:
      - "9100:9000"
      - "9103:9103"
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER:-mempalace}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD:-mempalace_secret}
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://127.0.0.1:9000/minio/health/live || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10

  # ── MemPalace API ────────────────────────────────────────────
  api:
    build: .
    ports:
      - "8100:8100"
    env_file:
      - path: .env
        required: false
    environment:
      - MEMPALACE_QDRANT_URL=http://qdrant:6333
      - MEMPALACE_PALACE_BASE=/data/palaces
      - MEMPALACE_DB_PATH=/data/users.db
      - JWT_SECRET=${JWT_SECRET:-change_this_in_production}
      - MEMPALACE_ADMIN_TOKEN=${MEMPALACE_ADMIN_TOKEN:-}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:3000,http://localhost:8080}
      # Storage: "local" (default) or "s3" (MinIO)
      - MEMPALACE_STORAGE=${MEMPALACE_STORAGE:-local}
      - S3_ENDPOINT=${S3_ENDPOINT:-http://minio:9000}
      - S3_ACCESS_KEY=${S3_ACCESS_KEY:-mempalace}
      - S3_SECRET_KEY=${S3_SECRET_KEY:-mempalace_secret}
      - S3_BUCKET=${S3_BUCKET:-mempalace}
      # LLM gateway (optional)
      - GATEWAY_URL=${GATEWAY_URL:-}
      - GATEWAY_API_KEY=${GATEWAY_API_KEY:-sk-mempalace}
      - GATEWAY_MODEL=${GATEWAY_MODEL:-openai/gpt-4o-mini}
    volumes:
      - palace_data:/data
      - fastembed_cache:/root/.cache/fastembed
    depends_on:
      qdrant:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://127.0.0.1:8100/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped

volumes:
  qdrant_data:
  palace_data:
  fastembed_cache:
  minio_data:
